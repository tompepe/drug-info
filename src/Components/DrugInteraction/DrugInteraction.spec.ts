// import React from 'react';
// import jest from 'jest';

describe('<DrugInteraction />', () => {
  // beforeEach(async () => {
  //     jest.clearAllMocks();

  //     renderTestObject();

  //     expect(await screen.findByTestId('page-header')).toHaveTextContent('Create Approval Routing');
  // });

  it('should render a page header and labels', () => {
    expect(true).toEqual(false);
  });
});
